# Python dependencies for the Ansible CONTROLLER (the host running
# `ansible-playbook`). Install with:
#
#     python3 -m venv .venv
#     . .venv/bin/activate
#     pip install -U pip
#     pip install -r controller-requirements.txt
#     ansible-galaxy collection install -r requirements.yml --force
#
# The ansible-core upper bound mirrors what the pinned ansible.posix 2.x
# was tested against; bump cautiously when a new minor is released.
ansible-core>=2.16,<2.21

# Linting / QA used in CI.
ansible-lint>=24.7.0,<26.0.0
yamllint>=1.35.1,<2.0.0

# Speeds up Jinja2 filters on large CIDR sets and is required by some
# modules' password_hash filter when generating yescrypt hashes locally.
jmespath>=1.0.1
passlib>=1.7.4
